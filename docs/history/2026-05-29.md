# تاريخ المهام: 2026-05-29

- تنفيذ `TASK-007`: تنفيذ واجهة قائمة السبايك والعملات (Bullions Feature).
- إنشاء `BullionModel` مع تهيئة Hive للعمل لاحقاً.
- إنشاء `BullionRepository` لجلب البيانات من الـ API.
- إنشاء `BullionProvider` (Riverpod) لإدارة الحالة.
- إنشاء `BullionScreen` (UI) لعرض قائمة السبايك والعملات.
- تحديث `progress.json` لتسجيل إتمام `TASK-007` والانتقال إلى `TASK-008`.

- تنفيذ `TASK-008`: تنفيذ واجهة وحاسبة الذهب الذكية المتقدمة (Calculator Feature & Dark AMOLED UI).
- إنشاء `CalculatorService` (OOP Class) لحساب أسعار الشراء وإعادة الشراء استناداً للمعادلات الموثقة والمركزية.
- إنشاء ثيم متكامل احترافي `AppTheme.amoledDarkTheme` باللون الأسود النقي والفيروزي العصري.
- تصميم شاشة البدء المتحركة `SplashScreen` وشاشة التنقل السفلية الموحدة `MainNavigationScreen` لربط شاشات التطبيق ببعضها.
- إعادة تصميم شاشات الأسعار `PriceScreen` والسبائك `BullionScreen` لتتوافق تماماً مع التصميم الفاخر AMOLED Dark.
- بناء شاشة الحاسبة الذكية المتقدمة `CalculatorScreen` التي تجلب أسعار الذهب تلقائياً لبلد المستخدم وتوفر حسابات دقيقة لكل من الشراء والبيع.
- كتابة اختبارات وحدة متكاملة لخدمة الحاسبة واختبار دخان (Smoke Test) لشاشة التطبيق الرئيسية والتحقق من عمل كافة الاختبارات بنجاح.
- تحديث `progress.json` لتسجيل إتمام `TASK-008` والانتقال إلى `TASK-009`.

- بناء الهيكل الجديد المتطور للتطبيق (10 شاشات احترافية - Premium AMOLED Financial Platform):
- تحديث وتطوير دليل الواجهات وشاشات التطبيق بالكامل في مستند `docs/07-mobile/app-screens-guide.md`.
- إنجاز المرحلة الأولى (Phase 1 - Auth & Onboarding):
  - بناء شاشة الترحيب والتعريف (`WelcomeScreen`) ذات التمرير التفاعلي مع أزرار التحكم والمتابعة كزائر.
  - بناء شاشة تسجيل الدخول (`LoginScreen`) الأنيقة بتأثير Glass Effect وخيارات تسجيل الدخول السريع والبريد الإلكتروني.
  - بناء شاشة إنشاء الحساب الجديد (`RegisterScreen`) مع التحقق والموافقة على الشروط.
  - تعديل الـ `SplashScreen` لربط تدفق التوجيه التلقائي بشاشات الترحيب والولوج.
- إنجاز المرحلة الثانية (Phase 2 - Financial Dashboard & Advanced Navigation):
  - دمج وتثبيت حزمة الرسم البياني المالي المتطور `fl_chart`.
  - بناء الشاشة الرئيسية (`DashboardScreen`) وتضمين الـ `Market Hero Section` لمتابعة حركة السوق، وبطاقات الأعيرة الفورية، وتحليلات السوق (Market Insights).
  - تطوير رسم بياني مالي تفاعلي متقدم يدعم عدة أطر زمنية (24 ساعة، أسبوع، شهر، سنة) لرصد تغيرات الأسعار بأسلوب احترافي مذهل.
  - بناء شاشة الملف الشخصي والإعدادات (`ProfileScreen`) لإدارة وتخصيص تجربة المستخدم (العملة، الدولة، الإشعارات، واللغة).
  - توسيع شريط التنقل السفلي (`MainNavigationScreen`) ليتنقل بمرونة كاملة عبر 5 تبويبات أساسية باستخدام الـ `IndexedStack` للحفاظ على الأداء وحالة الصفحات.
- إنجاز المرحلة الثالثة (Phase 3 - Live Market & Sparklines):
  - إعادة تصميم شاشة السوق والأسعار المباشرة (`PriceScreen`) لتضمين **شريط حالة السوق والتحليلات الأساسية** لتقدير مستوى التقلبات والنشاط واتجاه السوق اليومي.
  - تطوير ورسم **منحنيات بيانية مصغرة (Sparklines)** مدمجة ديناميكياً داخل بطاقات الأعيرة المالية المختلفة لعيار 24 و21 و18 لتوضيح السلوك السعري اليومي.
  - إضافة إحصائيات مالية دقيقة لكل عيار لعرض (أعلى سعر وأقل سعر تم تسجيله خلال اليوم) لتمكين المستخدمين والمهتمين من إجراء مقارنات سريعة وناجحة.
- إنجاز المرحلة الرابعة (Phase 4 - Advanced Explore Screen):
  - إعادة تصميم وتطوير شاشة استكشاف السبائك والمعادن (`BullionScreen` / Explore Screen) بالكامل.
  - إدخال نظام بحث مدمج (Searching) ونظام فلاتر تصفية ذكية متقدمة للفلترة السريعة حسب نوع المنتج (سبائك أو عملات) وحسب تصنيف الوزن بالجرام.
  - برمجة **ورقة تفاصيل تفاعلية (Product Details Bottom Sheet)** تفتح بسلاسة عند الضغط على أي عنصر وتعرض:
    - تفصيل كامل للمواصفات والتحليل الفني للمنتج (الوزن، العيار، النقاوة، والمنشأ).
    - رسم بياني تاريخي متحرك يوضح تغيرات السعر المرجعي للمنتج عبر الزمن باستخدام `fl_chart`.
    - نصائح استثمارية ذكية وتثقيفية مخصصة للادخار وحفظ القيمة.
- إنجاز المرحلة الخامسة (Phase 5 - Advanced Calculator & Share):
  - إعادة تصميم وتطوير شاشة الحاسبة بالكامل لتصبح حاسبة مالية تفاعلية ومباشرة (`CalculatorScreen`).
  - تفعيل **ميزة الحساب الفوري والتلقائي أثناء الكتابة (Live Calculation on Typing)** دون الحاجة للضغط على أي أزرار لتقديم تجربة مستخدم مدهشة وسريعة.
  - بناء **عرض التقسيم المالي المفصل (Breakdown Card)** لإظهار القيمة الأساسية للذهب، القيمة الإجمالية للمصنعية، والضرائب الإجمالية.
  - تفعيل **ميزة مشاركة النتائج وتصديرها (Share Results)** والتي تقوم بنسخ تفاصيل الحسبة المالية المنسقة بنصوص جذابة ومشاركتها بضغطة زر.
  - تفعيل **ميزة حفظ النتيجة محلياً** وتخزينها للمستقبل لتمكين المستخدمين من مقارنة حساباتهم.
  - تشغيل كافة اختبارات الوحدة والشاشات المحدثة بنجاح تام، ورفع جميع التعديلات والملفات المنجزة مباشرة على GitHub.

- إنجاز تعديلات جذرية على نظام المصادقة (Auth System Revamp):
  - تحويل النظام من الاعتماد على البريد الإلكتروني إلى **رقم الهاتف** كمعرف أساسي.
  - تحديث جداول الهجرة (`2026_05_29_215705_update_users_table_to_phone_number.php`) لتعديل جدول `users`.
  - تحديث الـ `AuthController` والـ `Models` والـ `Repositories` لدعم المصادقة عبر الهاتف.
  - تحديث واجهات الفلاتر (`LoginScreen`, `RegisterScreen`) لدعم إدخال رقم الهاتف وإجراء العمليات الحسابية عليه.
  - تنفيذ **نظام تأكيد (Confirmation Dialog)** عند تسجيل الخروج لضمان تجربة مستخدم آمنة ومنع الأخطاء غير المقصودة.
  - إنشاء Seeders جديدة (`GoldPriceHistorySeeder`) لتوليد بيانات تاريخية كثيرة (Big Data) لضمان أداء الرسوم البيانية.
  - إعادة بناء قاعدة البيانات وتنفيذ كافة الـ Seeders بنجاح.
