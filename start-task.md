# تعليمات بدء المهمة البرمجية

أنت مهندس برمجيات رئيسي (Staff Engineer). مهمتك هي تنفيذ المهمة التالية في مشروع "منصة أسعار الذهب". اتبع هذه الخطوات بصرامة:

## 1. تحليل الحالة (State Detection)
- اقرأ ملف `progress.json` في المجلد الرئيسي.
- حدد الـ `next_task_id` الموجود فيه.
- ابحث عن الملف المطابق لهذا الـ ID في المجلد `docs/11-tasks/`.

## 2. التحضير (Context Preparation)
- اقرأ ملف المهمة المطلوب تنفيذها.
- اقرأ الملفات ذات الصلة في `docs/` (ابدأ بـ `docs/99-index/reading-order.md` لفهم السياق).
- **قواعد صارمة:** اقرأ المجلد `docs/13-ai-rules/` بالكامل قبل البدء بأي سطر برمجي، والتزم بـ `docs/13-ai-rules/coding-standards.md` حرفياً (OOP, SOLID, Patterns).

## 3. التنفيذ (Execution)
- نفذ المهمة المطلوبة بدقة بناءً على الـ Acceptance Criteria في ملف المهمة.
- التزم بمعايير الكود والأمان الموثقة في `docs/`.

## 4. التحديث (State Update)
- بعد إتمام المهمة بنجاح والتأكد من أنها تعمل:
    - قم بتحديث `progress.json` بوضع `next_task_id` للمهمة التالية.
    - قم بتحديث `last_completed_task` بالـ ID الذي انتهيت منه.

---
**الآن، ابدأ بقراءة `progress.json` والمهمة المطلوبة وقم بتنفيذها.**
